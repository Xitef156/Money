{
  "main": "index.js",
  "scripts": {
    "start": "npm run ins && npm run un && npm run ins && node index.js",
    "un": "npm uninstall puppeteer && npm uninstall moment && npm uninstall npm-check-updates && npm uninstall discord-webhook-node && npm uninstall chromium",
    "ins": "npm install puppeteer && npm install moment && npm install npm-check-updates && npm install discord-webhook-node && npm install chromium"
  },
  "repository": {
    "type": "git"
  },
  "keywords": [
    "puppeteer"
  ],
  "dependencies": {
    "chromium": "^3.0.3",
    "discord-webhook-node": "^1.1.8",
    "moment": "^2.29.4",
    "puppeteer": "^19.2.2"
  },
  "engines": {
    "node": ">=14 <15",
    "npm": ">=6 <7"
  }
}
